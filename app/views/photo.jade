html
  body
    .wrap(ng-show="user.status=='login'",ng-class="{'show-toolbar': toolbar}")
      #menu
        .wrap
          a.nav(href="#/photo",ng-class="{active: tab=='photo'}") All Photos
          a.nav(href="#/album",ng-class="{active: tab=='album'}") Albums
        .toolbar-toggle(ng-class="{checked: toolbar}",ng-click="toolbar_toggle()")
      #toolbar
        .selected
          .item(ng-repeat="item in selected | lastTo:6",ng-class="{delete: item.hover}",ng-click="select(item)",ng-mouseover="item.hover=true",ng-mouseleave="item.hover=false",style="background-image:url({{item.src}});")
            .checkbox
        .select_info
          a.btn_blue(href="#/selected") {{selected.length}} Selected
          a.btn_red(ng-click="clear()") Delete All
        .next
          a.btn_blue(href="#/template") Next
      #main
        .item(ng-repeat="item in items | layout:box",ng-class="{hover: item.hover, active:item.selected}",ng-mouseover="item.hover=true",ng-mouseleave="item.hover=false",ng-click="select(item)",style="background-image:url({{item.src}});width:{{item.dis_width}}px;height:{{item.dis_height}}px")
          .checkbox